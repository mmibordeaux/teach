<%= semantic_form_for @promotion do |f| %>
  <%= f.input :year, label: 'Année' %>
  <%= f.input :calendar_url, label: 'URL du calendrier Google (Adresse privée au format iCal)' %>
  <%= f.submit 'Enregistrer', class: 'btn btn-default' %>
<% end %>
