<%= form_for(@teaching_unit) do |f| %>
  <% if @teaching_unit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@teaching_unit.errors.count, "error") %> prohibited this teaching_unit from being saved:</h2>

      <ul>
      <% @teaching_unit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :number %><br>
    <%= f.number_field :number %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
