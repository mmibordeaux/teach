<%= semantic_form_for @project do |f| %>
  <%= f.inputs do %>
    <%= f.input :label %>
    <%= f.input :description %>
    <%= f.input :fields, as: :check_boxes, label: 'Champs', collection: Field.not_root %>
    <%= f.input :semesters, as: :check_boxes, label: 'Semestres' %>
    <%= f.input :users, as: :check_boxes, label: 'Personnes'  %>
  <% end %>
  <%= f.actions %>
<% end %>
