<p id="notice"><%= notice %></p>

<h1><%= @users.count %> personnes</h1>
<%= link_to 'Nouvelle personne', new_user_path %>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Nom</th>
      <th>Responsable des modules</th>
      <th>Champs</th>
      <th>Projets</th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= link_to user, user %></td>
        <td><%= render partial: 'teaching_modules/inline_list', locals: {teaching_modules: user.teaching_modules} %></td>
        <td></td>
        <td></td>
      </tr>
    <% end %>
  </tbody>
</table>
